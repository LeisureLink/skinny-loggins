machine:
  timezone:
    America/Denver
  node:
    version: 4
test:
  override:
    - npm run ci
dependencies:
  pre:
    - rm -rf ~/$CIRCLE_PROJECT_REPONAME/node_modules
deployment:
  production:
    tag: /v.*/
    commands:
      - echo "This is where deployment would normally happen"
